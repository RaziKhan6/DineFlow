//
//  SampleTables.swift
//  DineFlow
//
//  Created by Razi Khan on 12/07/26.
//

import Foundation

enum SampleTables {

    static let tables: [Table] = [
        Table(
            number: 1,
            guestCount: 0,
            totalAmount: 0,
            elapsedMinutes: 0,
            status: .available
        ),
        Table(
            number: 2,
            guestCount: 0,
            totalAmount: 0,
            elapsedMinutes: 0,
            status: .available
        ),
        Table(
            number: 3,
            guestCount: 0,
            totalAmount: 0,
            elapsedMinutes: 0,
            status: .available
        ),
        Table(
            number: 4,
            guestCount: 0,
            totalAmount: 0,
            elapsedMinutes: 0,
            status: .available
        ),
        Table(
            number: 5,
            guestCount: 0,
            totalAmount: 0,
            elapsedMinutes: 0,
            status: .available
        )
    ]
}
